// @ts-nocheck

import { alorin, createSignal, createEffect } from "../core";

const App = () => {
  //   const [show, setShow] = createSignal(false);

  //   const [count, setCount] = createSignal(["dwe", "wed", "wed"]);
  //   const Drapper = ({ children }) => <div>{children("Hello")}</div>;
  //   //   const element = <Drapper>{greeting => <p>{greeting} World</p>}</Drapper>;
  //   return Drapper({ children: greeting => <p>{greeting} World</p> });

  return (
    <div>
      <p>Helldjfkvmkdjfvo!</p>
    </div>
  );
};

export default App;
